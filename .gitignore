# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# uv / mise
.mise.local.toml

# Experiment artefacts (regenerated)
results/
logs/
# results.jsonl / current_run.json / progress.html are regenerated; keep committed progress PNGs
experiments/
!experiments/progress/
!experiments/progress/**
# Hydra default run dir
outputs/
wandb/

# Env / secrets
.env
!.env.example
*.local

# locally-cloned reference, not part of this repo
lab-cookbook/

# large reference binary
*.pdf

# laguna_rlvr vendored into an env dir at push time (regenerated by scripts/submit_training.sh)
environments/*/laguna_rlvr/
