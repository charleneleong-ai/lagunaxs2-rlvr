# Points the Hydra rl group at the Prime-native training TOML that rl.py hands to `prime train`.
config: configs/rl/laguna-xs2.toml   # the actual training spec (Prime's schema)
max_steps: 50                        # mirrors the toml; informational for our gating/logging
